import "../../Fastfile"

default_platform(:android)

platform :android do
  desc "Build the Android APK locally with the project's own signing setup"
  lane :build do
    fetch_dependencies
    sh_on_root(command: "flutter build apk --release")
  end
end
