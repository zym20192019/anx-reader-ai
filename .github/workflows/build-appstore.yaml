# Legacy workflow intentionally disabled.
# This project does not use App Store Connect, Match, or store secrets in CI.
name: Legacy App Store build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: macos-latest
    steps:
      - run: echo "Disabled. Use release-independent.yaml."
