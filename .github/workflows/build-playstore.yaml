# Legacy workflow intentionally disabled.
# No Google Play or Firebase credentials are used by this standalone release.
name: Legacy Play Store build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use release-independent.yaml."
