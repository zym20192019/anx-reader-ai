# Legacy workflow intentionally disabled.
# Use release-independent.yaml for the standalone multi-platform release.
name: Legacy Android build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use release-independent.yaml."
