# Legacy workflow intentionally disabled.
# The standalone project publishes an unsigned Windows ZIP.
name: Legacy Windows build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - run: Write-Output "Disabled. Use release-independent.yaml."
