# Legacy workflow intentionally disabled.
# The standalone project publishes an unsigned IPA for local signing.
name: Legacy iOS build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: macos-latest
    steps:
      - run: echo "Disabled. Use release-independent.yaml."
