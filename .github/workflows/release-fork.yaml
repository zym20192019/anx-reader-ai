# Legacy Fork workflow intentionally disabled.
name: Legacy Fork release (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use release-independent.yaml."
