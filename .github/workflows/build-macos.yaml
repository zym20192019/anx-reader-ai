# Legacy workflow intentionally disabled.
# The standalone project publishes unsigned macOS ZIP/DMG artifacts.
name: Legacy macOS build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: macos-latest
    steps:
      - run: echo "Disabled. Use release-independent.yaml."
