# Legacy workflow intentionally disabled.
name: Legacy Windows manual build (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - run: Write-Output "Disabled. Use release-independent.yaml."
